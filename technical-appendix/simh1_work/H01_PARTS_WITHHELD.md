H01 raw shard part-files are withheld from this deposit version: the human replication
has not run yet (export 12/166 shards under sustained Google egress throttling).
They will be deposited together with the H01 H1-A/H1-B results when computed.
The pull/aggregate code (h01_aggregate_worker.py), shard manifest, and checkpoints
(h01_done.json, h01_worker_status.json) are included for provenance.
